{
  "name": "french-tutor-holdout-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:playwright": "npx playwright test",
    "test:vitest": "npx vitest run",
    "test:all": "npm run test:vitest && npm run test:playwright",
    "playwright:install": "npx playwright install --with-deps chromium firefox webkit"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@axe-core/playwright": "^4.9.0",
    "vitest": "^1.6.0",
    "playwright": "^1.45.0"
  }
}
